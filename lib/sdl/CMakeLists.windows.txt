cmake_minimum_required(VERSION 2.6)

install(
  DIRECTORY "${PROJECT_SOURCE_DIR}/include"
  DESTINATION .
  )

if (WIN32)
  install(
    DIRECTORY "${PROJECT_SOURCE_DIR}/lib/x86"
    DESTINATION lib
    )
else (WIN32)
  install(
    DIRECTORY "${PROJECT_SOURCE_DIR}/lib/x64"
    DESTINATION lib
    )
endif (WIN32)